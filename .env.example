# Optional: OpenAI for disambiguation and chain-of-thought
OPENAI_API_KEY=

# Optional: Langfuse/LangSmith for observability
OBS_API_KEY=

# Fulfillment webhook URL (default: mock warehouse on same backend)
FULFILLMENT_WEBHOOK_URL=http://localhost:8000/api/mock/warehouse

# Admin UI token (shared secret for admin panel)
ADMIN_TOKEN=demo-admin-token

# Backend: data directory (default: ./data)
# DATA_DIR=./data

# SQLite DB path (default: ./data/pharmguard.db)
# SQLITE_DB_PATH=./data/pharmguard.db
